apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name : "Expense"
  region: us-east-1
managedNodeGroups:
 - name: expense
   desiredCapacity: 3
   instanceType: "t3.micro"
   spot: true



